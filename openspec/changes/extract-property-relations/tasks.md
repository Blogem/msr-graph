# Tasks: extract-property-relations

## 1. Extraction project setup

- [x] 1.1 Add relation-extraction modules under `extraction/src/msr_extraction/`: Flash relation extractor + validator, unit→QUDT mapper, equation-form/coefficient parser, Python `measurement_value` writer, text-measurement triple writer, salt role/reactor edge writer (incl. a grounded `msr:MoltenSaltReactor` minter), and an `extract` CLI subcommand; reuse the existing `provenance.py` helper for generation provenance (do not add a second provenance path)
- [x] 1.2 Extend `config.py` additively: add the SQLite DB path and a confidence threshold (following the existing `MSR_*` env-var convention, e.g. `MSR_EXTRACT_CONFIDENCE_THRESHOLD`), and a `relations_path(report)` helper alongside the existing `mentions_path`/`segments_path`; reuse the existing `deepseek_base_url` / `deepseek_api_key` / `llm_model_extract` and `sparql_query_endpoint` / `sparql_update_endpoint`; keep all clients/paths injectable for tests
- [x] 1.3 Confirm the `extraction/` image still builds (no new heavy dependency beyond the chunk-6 DeepSeek client; stdlib `sqlite3` and `ontology/qudt-units.json` are already present)

## 2. Known schema + prompt reuse

- [x] 2.1 Reuse and **extend** chunk 6's `graph_reader.py` (`GraphReader`): its merged `read_known_entities()` exposes only concept/class/salt kinds (SKOS concepts, `owl:Class`, `PhysicalProperty`-as-`class`, `MoltenSalt`) — add a `read_salt_roles()` accessor for the reintroduced `msr:SaltRole` individuals (`FuelSalt`/`CoolantSalt`/`FlushSalt`, a **closed** set) plus a `PhysicalProperty`-specific accessor, so the run's closed known-IRI set covers salts, properties, and roles keyed by IRI (staging/proposal graphs never read); **do not** add a reactor accessor — reactors are minted from linked mentions (task 6.1), not validated against a known set; leave `read_known_entities()` unchanged to preserve chunk 6's NER seeding + byte-stable prompt prefix
- [x] 2.2 Reuse chunk 6's cached KG-schema prompt builder (`kg-schema-prompt`) as the Flash prefix; do not re-derive the TBox/vocab/salt-catalog serialization

## 3. Relation extraction (`relation-extraction`)

- [x] 3.1 Select the extraction inputs: curated-document sentences from `segments.jsonl` that carry ≥ 1 chunk-6 `status:"linked"` mention (from `data/corpus/{report#}/mentions.jsonl`), with the linked entities identified for the prompt
- [x] 3.2 Implement the injected OpenAI-compatible Flash extractor call (JSON output mode) over each selected sentence on top of the cached KG-schema prompt; parse the schema-constrained JSON app-side
- [x] 3.3 Treat the Flash output as a list of zero or more relations (multiple relations per sentence extracted independently); obtain a per-relation extraction confidence + rationale, and apply the configurable confidence threshold, marking below-threshold relations as skipped
- [x] 3.4 Validate each proposed relation's closed-set referents — salt ∈ loaded `MoltenSalt`, property ∈ seed `PhysicalProperty`, role ∈ seed `SaltRole` — rejecting any relation naming an unknown salt/property/role; a reactor referent is admitted only when its reference is a chunk-6 `linked` mention resolving to a surviving reactor `vocab.ttl` concept (grounding gate for minting, task 6.1), not validated against a closed set; drop malformed/schema-violating JSON with no partial write
- [x] 3.5 Write the `data/corpus/{report#}/relations.jsonl` trace artifact — one record per proposed relation with its confidence, rationale, and disposition (`written`/`rejected`/`skipped` + reason); deterministically regenerated per run (mirroring chunk 6's `mentions.jsonl`)

## 4. Unit → QUDT mapping (`unit-qudt-mapping`)

- [x] 4.1 Implement the unit-surface-form → canonical QUDT `unit:` IRI mapper driven by the vendored `ontology/qudt-units.json` (e.g. `cP`/`mPa·s`→`unit:MilliPA-SEC`, `g/cm³`→`unit:GM-PER-CentiM3`, `mN/m`→`unit:MilliN-PER-M`, `S/cm`→`unit:S-PER-CentiM`)
- [x] 4.2 Validate every mapped IRI against the allowlist and reject an unmappable/out-of-allowlist unit; cross-check the unit is dimensionally consistent with the extracted property via the property→canonical-unit map

## 5. Text-measurement writing (`text-measurement-writing`)

- [x] 5.0 Add the extraction-provenance TBox to `ontology/msr.ttl` — `msr:extractionConfidence` (datatype, `xsd:decimal`) and `msr:extractionRationale` (datatype, `xsd:string`), domain-agnostic (attach to a text-derived `msr:PropertyMeasurement` or a reified role/reactor edge); add the `rdf:` prefix if absent; keep it additive and rdflib-valid; ensure `make load-seed` (re-`PUT` of `urn:msr:ontology`) precedes `make extract`
- [x] 5.0a Reintroduce the role/reactor OWL TBox to `ontology/msr.ttl` (removed by `ground-demo-in-real-docs`, deferred to chunk 7): the `msr:SaltRole` class + closed individuals `msr:FuelSalt`/`msr:CoolantSalt`/`msr:FlushSalt`, `msr:hasRole` (`ObjectProperty`, domain `msr:MoltenSalt`, range `msr:SaltRole`), the `msr:MoltenSaltReactor` class, and `msr:usedIn` (`ObjectProperty`, domain `msr:MoltenSalt`, range `msr:MoltenSaltReactor`); additive + rdflib-valid; **no reactor individuals seeded** (they are minted, task 6.1); the role/reactor SKOS concepts already remain in `vocab.ttl` — do not re-add them
- [x] 5.1 Implement the equation-form/coefficient parser: map the extracted correlation to a seed `msr:EquationForm` (`Linear`/`Polynomial2`/`Polynomial3`/`Arrhenius`/`DiscretePoint`) and place coefficients into `c0..c4` (incl. `η = 0.084·exp(4340/T)` → Arrhenius `c0=0.084`, `c1=4340`; single value at T → DiscretePoint with `validTempMin=validTempMax`); reject on a coefficient-count/form mismatch; write `msr:validTempMin`/`Max` **both or neither**, ordered `min ≤ max` (a lone stated bound is dropped) so the write satisfies the merged SHACL `ValidTemperatureRangeShape`
- [x] 5.2 Derive the shared locator `doc/{report#}/{property}#{slug}` (canonical salt slug) and the deterministic measurement IRI from it (no blank nodes); ensure the same locator keys both stores
- [x] 5.3 Implement the text-measurement triple writer: `msr:PropertyMeasurement` with `msr:ofSalt`, `msr:forProperty`, `msr:hasUnit`, `msr:equationForm`, `msr:validTempMin`/`Max`, `msr:dataLocator`, `prov:wasDerivedFrom` the source `Document`, `prov:wasGeneratedBy msrd:activity-extraction` (the stable per-pipeline activity from `provenance.py`), `msr:citedIn`, and the queryable `msr:extractionConfidence` + `msr:extractionRationale` → `urn:msr:data` via additive `INSERT DATA` through the chunk-5 `SparqlClient` (never `PUT`)
- [x] 5.6 Wire generation provenance by reusing `provenance.py` (mirroring chunk 6's `link`): mint one `run_ts` per `extract` invocation, call `write_stable_activity()` (types `msrd:activity-extraction` in `urn:msr:data`) and `write_activity(run_ts)` (writes the per-run `<urn:msr:run:extraction/{run_ts}>` `prov:Activity` node into `urn:msr:provenance`) **before** any fact write, and emit one `<individual> prov:wasGeneratedBy <urn:msr:run:extraction/{run_ts}>` per-run generation edge into `urn:msr:provenance` for every asserted individual (each measurement, each minted reactor, each `rdf:Statement` reification node); bump `EXTRACTION_VERSION` for the new stage
- [x] 5.4 Implement the Python `measurement_value` writer via stdlib `sqlite3` through a connection helper pinning `journal_mode=DELETE` + non-zero `busy_timeout` (no WAL sidecars); insert rows with `source='document'`, `doc_id`, canonical `salt`, `property`, `equation_form`, `t_min`/`t_max`, `uncertainty`, `c0..c4`; upsert on the `locator` primary key
- [x] 5.5 Skip (and record in the run summary) any measurement whose salt resolved only to a bare concept, never guessing a composition
- [x] 5.7 Additively extend the shared `sparql.py` `SparqlClient.update()` to classify a GraphDB SHACL commit rejection (RDF4J validation-report response) as a typed `ValidationError` carrying the report, distinct from a generic transport error — satisfying the `shacl-validation` "reports legible to writers" requirement for extraction writers (mirrors the Go loader's `ValidationError`); the success path is unchanged (chunk 6 also uses this helper)

## 6. Salt role / reactor edges (`salt-role-reactor-edges`)

- [x] 6.1 Write validated `msrd:{salt} msr:hasRole msr:{Role}` (role ∈ the closed seed `msr:SaltRole` set) and `msrd:{salt} msr:usedIn msrd:reactor-{slug}` direct edges to `urn:msr:data` via additive `INSERT DATA`; for `usedIn`, **mint** the `msr:MoltenSaltReactor` individual `msrd:reactor-{slug}` (deterministic IRI, `a msr:MoltenSaltReactor`, `rdfs:label`, a link to its grounding `vocab.ttl` reactor concept, and generation provenance per task 5.6) from the chunk-6-linked reactor mention; for every text-derived edge also write a deterministic `rdf:Statement` reification (`rdf:subject`/`rdf:predicate`/`rdf:object`) carrying `msr:extractionConfidence` + `msr:extractionRationale` + `prov:wasDerivedFrom`/`prov:wasGeneratedBy` (direct edge preserved unchanged for the agent); no blank nodes
- [x] 6.2 Reject a role edge naming an unknown role individual; a `usedIn` whose reactor reference is not a chunk-6 `linked` mention to a reactor concept produces no edge (no minting from thin air); ensure re-asserting an existing edge, reification node, and minted reactor is a set-semantics no-op

## 7. `extract` orchestration, wiring & docs

- [x] 7.1 Implement the `extract` CLI umbrella: mint `run_ts` + write stable/per-run provenance (task 5.6) → load known schema + prompt → select mention-bearing sentences → extract + validate → write measurements (both stores) + role/reactor edges (+ minted reactors) with per-fact generation edges, over the curated set; print a run summary (per doc: sentences seen, relations extracted, measurements/edges/reactors written, rejected/skipped counts)
- [x] 7.2 Add the `make extract` target (`docker compose run --rm extraction extract`, mirroring `link:` — **no** `load-seed` prerequisite; add `extract` to the Makefile `.PHONY` list); update the README bootstrap order to `up → load-nist (runs load-seed) → ingest → link → extract` (note `load-seed` now PUTs only the TBox/vocab and is **safe** to re-run after data is loaded — it no longer touches `urn:msr:data`; re-running it is how a `msr.ttl` TBox edit is applied); document the new `source='document'` rows + text-derived `PropertyMeasurement` nodes (with confidence/rationale) + minted `msr:MoltenSaltReactor` individuals + role/reactor edge reifications + the per-run lineage in `urn:msr:provenance` + the `relations.jsonl` trace artifact

## 8. Tests

- [x] 8.1 Stubbed-Flash relation-extraction tests over fixture sentences → expected validated relations (salt + property + value + unit, incl. the Arrhenius `η = 0.084·exp(4340/T)` case and a DiscretePoint value-at-T case); relations naming an unknown salt/property/reactor/role IRI or an out-of-allowlist unit are rejected; malformed JSON → dropped, no write
- [x] 8.2 Sentence-selection tests: only sentences carrying a `status:"linked"` mention are sent to Flash; a mention-free sentence is skipped
- [x] 8.3 Unit→QUDT mapping table tests (surface form → canonical `unit:` IRI); unmappable/out-of-allowlist rejected; property-vs-unit dimensional-consistency rejection
- [x] 8.4 Equation-form/coefficient parsing tests: each form → correct `msr:EquationForm` + `c0..c4`; coefficient-count/form mismatch rejected
- [x] 8.5 Measurement dual-store write tests: a validated measurement → the exact expected `INSERT DATA` triples (deterministic IRI, `msr:citedIn`, `prov:wasDerivedFrom` the `Document`, `prov:wasGeneratedBy msrd:activity-extraction`, no blank nodes) against a fake SPARQL client, and the exact `measurement_value` row (`source='document'`, shared locator, coefficients) against a temp SQLite DB; re-run leaves both unchanged (idempotency); assert no `-wal`/`-shm` sidecar after the write and `journal_mode=delete`
- [x] 8.6 Role/reactor edge tests: a validated role statement → expected direct `hasRole` triple to a reintroduced `msr:SaltRole` individual + its `rdf:Statement` reification carrying queryable `msr:extractionConfidence`/`msr:extractionRationale` (+ generation provenance); a `usedIn` statement whose reactor reference is a chunk-6 `linked` mention **mints** the deterministic `msr:MoltenSaltReactor` individual (typed, `rdfs:label`, grounding-concept link, `prov:wasDerivedFrom`/`prov:wasGeneratedBy`) and the edge + reification; an unknown role is rejected; a reactor reference that is not a linked mention yields no `usedIn` edge and mints nothing; re-assert leaves edge, reification-node, and minted-reactor counts unchanged
- [x] 8.7 Core-dataset read guard test: a salt/property present only in `urn:msr:staging` is not in the known-IRI set and is never a valid extraction referent
- [x] 8.8 Bare-concept skip test: a measurement statement whose salt resolved to a concept (no composition) writes nothing and is recorded as skipped
- [x] 8.9 Multiple-relations-per-sentence test: a sentence asserting two relations (e.g. a role and a reactor) yields two, each validated and written independently; an empty relation list writes nothing
- [x] 8.10 Confidence/rationale tests: a written measurement carries `msr:extractionConfidence` + `msr:extractionRationale` queryable in `urn:msr:data` while a NIST measurement carries neither; a written relation is recorded in `relations.jsonl` with confidence + rationale + `disposition:"written"`; a below-threshold relation is `skipped` (nothing written); a validation failure is `rejected` with its reason; the physical `uncertainty` string (when present) lands in the SQLite column, separate from the extraction confidence
- [ ] 8.11 Guarded integration test (opt-in env flag, mirroring chunk 1's `GRAPHDB_REQUIRED`) against a **SHACL-enabled** `msr` repo: after seed + catalog + `link` + a real `extract` over ORNL-TM-2316, a known FLiBe viscosity statement becomes a `PropertyMeasurement` with its value in `measurement_value` and `msr:citedIn msrd:ORNL-TM-2316` — the write **accepted by SHACL** (confirming the seven required properties + allowlisted unit) — the unchanged chunk-4 agent answers a question using it, and a second `extract` run leaves both fact stores' counts unchanged (while `urn:msr:provenance` gains a second per-run activity)
- [x] 8.12 Generation-provenance tests (reuse `provenance.py`, stub `run_ts`): a written measurement, a minted reactor, and a reification node each carry `prov:wasGeneratedBy msrd:activity-extraction` + `prov:wasDerivedFrom` in `urn:msr:data`; the `extract` run writes exactly one per-run `prov:Activity` node `<urn:msr:run:extraction/{run_ts}>` (attributed + timestamped) and one generation edge per asserted fact into `urn:msr:provenance`, the activity node written before any generation edge; a second run (distinct `run_ts`) appends a second per-run activity + edge set while `urn:msr:data` triple counts and `measurement_value` row counts are unchanged
- [x] 8.13 Reintroduced-TBox test: `ontology/msr.ttl` parses (rdflib) and declares `msr:SaltRole` + `FuelSalt`/`CoolantSalt`/`FlushSalt`, `msr:hasRole` (domain `MoltenSalt`, range `SaltRole`), `msr:MoltenSaltReactor`, and `msr:usedIn` (domain `MoltenSalt`, range `MoltenSaltReactor`); no reactor individuals are seeded; the role/reactor SKOS concepts still resolve in `vocab.ttl`
- [x] 8.14 SHACL-conformance tests (hermetic): the emitted measurement triples carry all seven `PropertyMeasurementShape`-required properties (`prov:wasDerivedFrom`, `prov:wasGeneratedBy`, `msr:dataLocator`, `msr:forProperty`, `msr:ofSalt`, `msr:hasUnit`, `msr:equationForm`) and only an allowlisted `msr:hasUnit`; a temperature range is emitted both-bounds-or-neither and ordered; the minted reactor uses a non-`msr:linksTo` predicate for its grounding-concept link (no `LinksToTargetKindShape`/domain trip)
- [x] 8.15 `sparql.py` ValidationError test: a simulated GraphDB SHACL validation-report response makes `SparqlClient.update()` raise the typed `ValidationError` (carrying the report), distinct from a generic transport error; a 2xx response still succeeds unchanged

## 9. Manual acceptance run

- [ ] 9.1 After a full bootstrap (`make up` → `make load-nist` (runs `load-seed` then the NIST load) → `ingest` → `link` → `extract`), do a real end-to-end run over the actual curated documents and manually inspect the output: confirm a known FLiBe viscosity statement in ORNL-TM-2316 became a `PropertyMeasurement` (with `msr:citedIn` + `prov:wasGeneratedBy`) whose value is in `measurement_value` (`source='document'`), spot-check role edges and any minted `msr:MoltenSaltReactor` individuals (e.g. the ORNL-TM-2316 MSRE-coolant statement) with their reifications, confirm per-run lineage landed in `urn:msr:provenance`, verify the chunk-4 agent now answers the text-derived value unchanged, and confirm a second `extract` run adds no duplicates to `urn:msr:data`/`measurement_value` — noting the `msr` repo enforces SHACL, so a successful run also proves the writes pass the installed shapes — the change is done only after this manual verification passes, not on green tests alone
