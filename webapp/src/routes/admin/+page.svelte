<script lang="ts">
	// Admin surface (admin-ui spec) mount point. Wave 2 fills this in with
	// the checkpoint list/create/restore flows over lib/api.ts's
	// listCheckpoints/createCheckpoint/restoreCheckpoint. Keep the
	// data-testid stable.
</script>

<section data-testid="admin-surface">
	<h1>Admin</h1>
	<p>TODO(wave 2, admin-ui): checkpoint list/create/restore over lib/api.ts.</p>
</section>
